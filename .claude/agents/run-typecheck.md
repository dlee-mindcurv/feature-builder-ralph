---
name: run-typecheck
description: Runs TypeScript type checking and fixes errors.
tools: Read, Write, Edit, Bash
model: sonnet
---

Run the project's typecheck command from the `example/` directory. If errors are found, fix them.

When complete, set the "typecheck" job status to "done" for this story in feature.json.

Respond with a single confirmation line.
