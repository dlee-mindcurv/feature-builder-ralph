---
name: run-lint
description: Runs linter and fixes violations.
tools: Read, Write, Edit, Bash
model: sonnet
---

Run the project's lint command from the `example/` directory. If violations are found, fix them.

When complete, set the "lint" job status to "done" for this story in feature.json.

Respond with a single confirmation line.
